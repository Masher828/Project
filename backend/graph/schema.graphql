type Query{
    getUser(id:ID): User
}

type User{
    _id: ID
    name:String
}

input userInput{
    name:String
}

input messageInput{
    body : String,
    from : ID,
    to : ID
}

type Mutation{

    addUser(input:userInput): ID
    sendMessage(input:messageInput): String

}
